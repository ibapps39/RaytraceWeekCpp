#pragma once
//# ifndef MAIN_HPP
#include <iostream>
#include <cmath>

#define PPM_MAX_VAL 255
#define PPM_MIN_VAL 0

// extern unsigned char min_val;
// extern unsigned char max_val;

void makeExamplePPM(unsigned char min_val, unsigned char max_val);
class vec3;

//#endif